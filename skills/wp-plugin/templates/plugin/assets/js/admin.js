/* {{PLUGIN_NAME}} — JS do painel. Código original. */
(function () {
  'use strict';
  // Exemplo de chamada AJAX usando o objeto localizado {{PREFIX}} (ajax + nonce).
  // function {{prefix}}Do(payload) {
  //   var body = new URLSearchParams(Object.assign({ action: '{{prefix}}_do', nonce: {{PREFIX}}.nonce }, payload));
  //   return fetch({{PREFIX}}.ajax, { method: 'POST', credentials: 'same-origin', body: body })
  //     .then(function (r) { return r.json(); });
  // }
})();
